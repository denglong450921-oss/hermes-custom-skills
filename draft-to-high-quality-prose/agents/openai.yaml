interface:
  display_name: "Draft to High Quality Prose"
  short_description: "Rewrite drafts and polish HTML presentation"
  default_prompt: "Use $draft-to-high-quality-prose to rewrite this draft and, if requested, turn it into a polished HTML reading page."
