interface:
  display_name: "Draft to High Quality Prose"
  short_description: "Rewrite drafts and save Markdown/HTML"
  default_prompt: "Use $draft-to-high-quality-prose to rewrite this draft, save Markdown and HTML outputs, copy the Markdown to the liuskill vault, and open the HTML preview."
